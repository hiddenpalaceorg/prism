//! Curator native Windows GUI (windows-rs) — scaffold.
//! See README.md for the build plan. Calls `curator-core` directly in-process.

fn main() {
    eprintln!(
        "curator-gui-win is a scaffold — not yet implemented. See crates/curator-gui-win/README.md"
    );
}
